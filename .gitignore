# Dependencies
node_modules/

# Build output
dist/
*.tsbuildinfo

# Environment variables (SENSITIVE - never commit!)
.env
.env.local
.env.*.local

# Output files (contains passwords and sensitive data)
output/

# State tracking files
state/

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OID cache files
*_oid_cache.json

# Claude Code settings (contains local paths)
.claude/

# OS files
.DS_Store
Thumbs.db

# IDE / Editor
.vscode/
.idea/
.windsurf/
*.swp
*.swo
*~

# Temporary files
tmp/
temp/
*.tmp
/debug/

# Admin tools — tracked in git (credentials come from .env, not these scripts)

# Token cache (stored in home directory, but just in case)
token-cache.json

# Internal data files (contain real tenant data — never commit)
config/users.config.json
config/users.config.backup.json
config/users.config.diff
config/profile-data.json
config/textcraft-europe.csv
config/textcraft-europe.json
config/sample-rich.json

# Credentials
cred.txt

# Nested repos / scratch
napkin/

# gstack
.gstack/
